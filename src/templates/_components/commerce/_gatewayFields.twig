<input type="{{ inputType ?? '' }}" name="accountId" value="{{ accountId ?? paymentForm.accountId ?? null }}" />
<input type="{{ inputType ?? '' }}" name="accountReference" value="{{ accountReference ?? paymentForm.accountReference ?? null }}" />
